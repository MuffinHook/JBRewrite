return {
    teamId = require(game:GetService("ReplicatedStorage").Resource.Settings).EnumTeam.Criminal;
    message = "Rob two small stores (gas station, donut) without dying!";
    goal = 2;
    rewardExp = 30;
}